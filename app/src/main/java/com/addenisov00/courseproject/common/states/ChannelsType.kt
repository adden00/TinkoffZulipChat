package com.addenisov00.courseproject.common.states

import com.addenisov00.courseproject.R

enum class ChannelsType(val value: Int) {
    MY_CHANNELS(R.string.subscribed), ALL_CHANNELS(R.string.All_streams)
}